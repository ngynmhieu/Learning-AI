# LLM access — the backend calls the standalone models service over an
# OpenAI-compatible API. No torch/transformers/bitsandbytes here anymore;
# the model runs in the models service.
openai

# Server / API
fastapi>=0.95.0
uvicorn[standard]>=0.22.0
python-dotenv

# Database (Supabase Postgres via session pooler)
sqlalchemy[asyncio]>=2.0
asyncpg

# Auth (Supabase — verifies JWTs, reads the user behind a token)
supabase

# Optional utilities
requests
