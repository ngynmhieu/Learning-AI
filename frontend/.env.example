BACKEND_HOST=localhost
BACKEND_PORT=8000
# Points at the CLI-managed local Supabase stack (`supabase start`).
# Get the anon key from `supabase status` after starting it.
VITE_SUPABASE_URL=http://localhost:54321
VITE_SUPABASE_ANON_KEY=eyJ...
